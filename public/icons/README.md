# Icons Directory

This folder should contain the following icon files used by the application:

- `icon-192.png` (192x192 pixels)
- `icon-512.png` (512x512 pixels)

The application is configured to load these files automatically from this location.